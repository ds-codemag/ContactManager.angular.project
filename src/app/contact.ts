export class Contact {
    id: number;
    name: string;
    number: string;

    constructor(id, name, number) {
        this.id = id;
        this.name = name;
        this.number = number;
    }
}
